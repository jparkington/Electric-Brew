from utils import meter_usage

print(meter_usage)

# Explain what PYTHONPATH is doing now for making `utils` importable anywhere in `src`
# Explain how `read_data` only need imports relative to `/data/` now