from utils import *

# Replace `dim_suppliers`
# Ampion
# ['invoice_number', 'account_number', 'interval_start', 'interval_end', 'supplier'
# ['kwh_delivered', 0, 0,  df['ampion_price'] / df['ampion_kwh'] = `supply_rate`

# CMP
# 'invoice_number', 'account_number', 'interval_start', 'interval_end', 'supplier'
# 'kwh_delivered', 'service_charge', 'delivery_rate', 'supply_rate'

print(dim_bills)
